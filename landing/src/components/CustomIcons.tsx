import * as React from 'react';
import { SvgIcon, SvgIconProps, } from '@mui/material';

const TestIcon: React.FunctionComponent<SvgIconProps> = () => {
    return (
        <SvgIcon>
        </SvgIcon>
    );
};

export { TestIcon };
